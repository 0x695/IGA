{
  "name": "iga",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Impressions Games Archive - a reference site for the Impressions Games / Sierra City Building series and its open-source engine ecosystem",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "check": "astro check",
    "images": "node scripts/optimize-images.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "pagefind": "^1.1.0",
    "typescript": "^5.6.0"
  }
}
